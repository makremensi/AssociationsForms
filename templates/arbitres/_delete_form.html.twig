<form method="post" action="{{ path('arbitres_delete', {'id': arbitre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ arbitre.id) }}">
    <button class="btn">Delete</button>
</form>
